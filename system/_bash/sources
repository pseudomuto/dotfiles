[[ $OSTYPE == darwin * ]] && source /usr/local/share/chruby/chruby.sh

# legacy rbenv...
[ -d ~/.rbenv ] && RUBIES+=(~/.rbenv/versions/*)

[[ $OSTYPE == darwin* ]] && source /usr/local/share/chruby/auto.sh
