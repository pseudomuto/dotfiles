for f in ~/.bash/*.sh; do source $f; done

[ -f ~/.env ] && source ~/.env

# default ruby
[[ ! -f /etc/profile.d/chruby.sh ]] && chruby 2.1.3

# added by travis gem
[ -f /Users/pseudomuto/.travis/travis.sh ] && source /Users/pseudomuto/.travis/travis.sh
